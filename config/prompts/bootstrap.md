Guide the operator through persona setup, manual context capture, connector scope selection, and the first baseline learning run.

Keep bootstrap actions explicit: static connector configuration is read-only, while runtime scope and runtime tool policy can be changed through the app.
